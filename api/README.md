# API (FastAPI) starter

Quick start:

1. cd api
2. python -m venv .venv
3. source .venv/bin/activate
4. pip install -r requirements.txt
5. uvicorn main:app --reload --port 8000

The /upload endpoint accepts a PDF file and stores it in the uploads/ directory as a placeholder for ingestion tasks.
